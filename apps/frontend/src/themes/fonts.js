const fallback = '-apple-system, BlinkMacSystemFont, system-ui, "Segoe UI", Roboto, "Helvetica Neue", Ubuntu, Arial, sans-serif';

const primary = `'Montserrat', 'Open Sans', ${fallback}`;
const secondary = `'Open Sans', ${fallback}`;

const fonts = {
  fallback,
  primary,
  secondary,
};

export default fonts;
